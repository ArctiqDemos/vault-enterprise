storage "raft" {
  path    = "/opt/vault/data"
  node_id = "<%= @node_id %>"
}

listener "tcp" {
  address     = "0.0.0.0:8200"
  tls_disable = 1
}

api_addr = "<%= @api_addr %>"
cluster_addr = "<%= @cluster_addr %>"
ui = true
